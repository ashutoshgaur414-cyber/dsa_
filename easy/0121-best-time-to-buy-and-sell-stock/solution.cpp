      }

        minprice = min(minprice,prices[i]);

      if(maxprofit<=0)
      {
        return 0;
      }
      else
      {
        return maxprofit;
      }
        maxprofit = max(maxprofit,prices[i]-minprice);
      {
      for(int i = 0;i<prices.size();i++)
    }
};
