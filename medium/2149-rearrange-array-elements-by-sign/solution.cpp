     for (int i = 0;i<nums.size();i++)
         {
            if(nums[i]>0)
            {
                ans.at(pos)=nums[i];
        int neg = 1;
        int pos = 0;
    vector<int> rearrangeArray(vector<int>& nums) {
public:
class Solution {
        vector<int>ans(nums.size());
